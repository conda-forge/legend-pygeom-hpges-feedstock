{% set name = "legend-pygeom-hpges" %}
{% set version = "0.8.0" %}
{% set python_min = "3.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/legend_pygeom_hpges-{{ version }}.tar.gz
  sha256: d00a737e620c1d93bdb28367a5e9378031f2619802e15f3114d188f60fdfd9d4

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=61.2
    - setuptools-scm >=7
    - pip
  run:
    - python >={{ python_min }}
    - numpy
    - numba
    - pint !=0.24
    - pyg4ometry
    - dbetto
    - awkward

test:
  imports:
    - legendhpges
  commands:
    - pip check
  requires:
    - python {{ python_min }}
    - pip

about:
  summary: Geometry management for LEGEND HPGe detectors
  home: https://github.com/legend-exp/legend-pygeom-hpges
  license: GPL-3.0-only
  license_file: LICENSE
  doc_url: https://legend-pygeom-hpges.readthedocs.io
  dev_url: https://github.com/legend-exp/legend-pygeom-hpges

extra:
  recipe-maintainers:
    - gipert
    - ManuelHu
